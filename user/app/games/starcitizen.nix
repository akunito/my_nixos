{ pkgs, pkgs-stable, pkgs-unstable, ... }:

{

  # home.packages = [ 
  #   # in Wayland, run from terminal by: $ env GDK_BACKEND=x11 lug-helper
  #   pkgs-unstable.lug-helper # Star Citizen launcher 
  # ];

}