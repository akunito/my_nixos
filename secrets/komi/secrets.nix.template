{
  # Komi's personal secrets
  # To use: copy to secrets.nix and fill in real values
  # Then run: git-crypt init --key-name komi
  #           git-crypt export-key --key-name komi ~/komi-git-crypt-key
  gitEmail = "komi@example.com";
  # Add more as needed
}
